{
  "mode": "analyze",
  "task": "Verify Schema Consistency for Auth System",
  "success_criteria": [
    "Both agents agree on ID type (UUID vs Serial)",
    "Ensure email uniqueness is enforced",
    "Verify password hashing column exists"
  ],
  "sources": [
    {
      "agent": "gemini",
      "session_id": "schema-plan",
      "cwd": "fixtures/demo"
    },
    {
      "agent": "claude",
      "session_id": "schema-impl",
      "cwd": "fixtures/demo"
    }
  ],
  "constraints": [
    "Must use UUIDs for security",
    "PostgreSQL compatibility"
  ]
}
